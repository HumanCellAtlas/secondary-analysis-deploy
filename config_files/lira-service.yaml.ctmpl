apiVersion: v1
kind: Service
metadata:
    name: {{ env "LIRA_SERVICE_NAME" }}
spec:
    type: ClusterIP
    selector:
        app: {{ env "LIRA_APPLICATION_NAME" }}
    ports:
        - port: 8080
          targetPort: 8080
          protocol: TCP
